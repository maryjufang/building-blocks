{{#svg-tooltip-wrapper as |wrapper|}}
  {{#if wrapper.target}}
    {{component iconComponentName width=width height=width radius=radius dataTestId=dataTestId status=status}}
  {{/if}}
  {{#if (and wrapper.tip tooltip)}}
    {{tooltip}}
  {{/if}}
{{/svg-tooltip-wrapper}}
