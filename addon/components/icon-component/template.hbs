{{#svg-tooltip-wrapper as |wrapper|}}
  {{#if wrapper.target}}
    {{component iconComponentName widthHeight=widthHeight dataTestId=dataTestId status=status}}
  {{/if}}
  {{#if (and wrapper.tip tooltip)}}
    {{tooltip}}
  {{/if}}
{{/svg-tooltip-wrapper}}
